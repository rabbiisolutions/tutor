const signUp = 'Sign Up';
const menuTitle = 'Menu';
const menuItems = ['Home', 'Payment', 'Become a Tutor', 'About Us', 'Trending', 'FAQs'];
const titles = ['Types of teachers', 'why rabbii', 'tuition areas', 'get a discount'];
const hidden = 'hidden';

const texts = {
  signUp: signUp, menuTitle: menuTitle,
  menuItems: menuItems, hidden: hidden, titles: titles
};

export default texts;
